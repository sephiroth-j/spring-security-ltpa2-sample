spring:
    ldap:
        embedded:
            baseDn: dc=foo,dc=bar
            port: 33389

logging:
    level:
        org:
            springframework:
                security:
                    ldap: DEBUG
        hello:
            auth: DEBUG
            